import { GetIdFromNamePipe } from './get-id-from-name.pipe';

describe('GetIdFromNamePipe', () => {
  it('create an instance', () => {
    const pipe = new GetIdFromNamePipe();
    expect(pipe).toBeTruthy();
  });
});
