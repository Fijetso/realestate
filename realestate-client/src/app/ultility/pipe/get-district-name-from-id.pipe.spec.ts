import { GetDistrictNameFromIdPipe } from './get-district-name-from-id.pipe';

describe('GetDistrictNameFromIdPipe', () => {
  it('create an instance', () => {
    const pipe = new GetDistrictNameFromIdPipe();
    expect(pipe).toBeTruthy();
  });
});
